//main.rs

use std::{env, fs};
use queue_sim::*;

fn main() {
    //check CL args
    //file IO
    //sim: loop for 1 to n-1 processors
      //init vars
      //run sim for n processors
        //check for dequeues, execute as necessary
        //check for enqueues, execute as necessary
        //repeat until done
      //calc wait time data
      //print iteration stats
}
